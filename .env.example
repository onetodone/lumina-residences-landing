# Public site URL, used for metadataBase (Open Graph / Twitter image resolution).
# No real domain exists yet for this portfolio project — leave unset locally.
NEXT_PUBLIC_SITE_URL=https://example.com
